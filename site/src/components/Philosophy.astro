---
---
<section class="sec">
  <div class="sec-label">What we didn't build</div>
  <div class="phil">
    <div><span class="no">A message bus</span> — agents coordinate through the log, in git.</div>
    <div><span class="no">A central server</span> — the repo is the infrastructure.</div>
    <div><span class="no">Prompt-engineered "alignment"</span> — <b>two rules</b>, enforced by code.</div>
    <div class="phil-foot">Everything else is your call. That's the point.</div>
  </div>
</section>

<style>
  .phil { max-width: 560px; margin: 0 auto; font-size: 12.5px; line-height: 2.1; color: #9fb3c8; }
  .phil .no { color: var(--dimmer); text-decoration: line-through; text-decoration-color: #3d4754; }
  .phil b { color: var(--yellow); font-weight: 400; }
  .phil-foot { margin-top: 10px; font-size: 11px; color: var(--dimmer); }
</style>

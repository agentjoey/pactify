{
  "fixedSectionOk": true,
  "nodesRecovered": 12,
  "edgesRestored": 48,
  "crossBatchEdgesAdded": 31,
  "typesRemapped": 1,
  "complexityRemapped": 0,
  "notes": [
    "Sanity check: merge script applied only 17 mechanical fixes (all tested_by edge direction flips). Zero ID-normalization and zero complexity-remap work needed — the LLM output was already well-formed; no systemic issues.",
    "Step 2a: added 7 minimal `file:` nodes for cloud/relay/src/* files missing entirely from the graph (server.ts, sockets.ts, redis.ts, repo.ts, retention.ts, rateLimit.ts, server-main.ts). These were the only file: targets that existed on disk but had no graph representation.",
    "Step 2b: added 5 minimal `function:` nodes for targets of dropped `calls` edges (4 in web/src/lib/api.ts, 1 in internal/orchestrate/gate.go). Functions are exported and on disk; they were simply not enumerated by any file-analyzer batch.",
    "Step 2c: one `contains` edge had a malformed target ID — `file:cloud/relay/src/broadcast.ts:createBroadcaster` (file: prefix on a function reference). Re-targeted to `function:cloud/relay/src/broadcast.ts:createBroadcaster`.",
    "Step 2d: re-targeted 15 `documents`/`related` edges whose targets existed under a different prefix (document:, config:, pipeline:, schema:). Includes 6 docs→AGENTS.md/CLAUDE.md/GEMINI.md edges re-targeted to `document:` nodes, 2 → pipeline:.github/workflows/ci.yml, 2 → config:web/vercel.json, and several .pact/tasks/* → package.json/toml re-targetings.",
    "Step 2e: re-targeted one `deploys` edge from `file:cloud/relay/prisma/schema.prisma` to the existing `schema:` node.",
    "Step 2f: fixed malformed `tags` field on `function:internal/pact/merge_empty_test.go:TestMergeRefusesEmptyFeatureBranch` (it had been a string instead of a list). The node's `complexity` was already defaulted to `moderate` by the merge script. This counted toward typesRemapped since it normalized a field, not a node type.",
    "Step 3: ran importMap recovery (now that all 7 relay source file: nodes exist). 31 new imports edges were added, all importMap-backed (verified programmatically). No speculative edges added.",
    "Final graph: 2721 nodes (+12), 7566 edges (+79). 0 dangling edges, 0 duplicate edges."
  ]
}
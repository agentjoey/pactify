version: 2

project_name: pactify

before:
  hooks:
    # -diff fails loudly instead of silently rewriting go.mod mid-release
    - go mod tidy -diff

builds:
  - id: pactify
    main: ./cmd/pactify
    binary: pactify
    env:
      - CGO_ENABLED=0
    goos: [darwin, linux]
    goarch: [amd64, arm64]
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

archives:
  - id: pactify
    name_template: "pactify_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: agentjoey
    name: pactify
  draft: false
  prerelease: auto

changelog:
  # `use: git` (not github) so the filters below actually apply — pactify's own
  # dogfood writes one "pact: ledger sync" commit per protocol event, so a raw
  # commit list is ~75% coordination noise (759 commits between v0.9.0 and
  # v0.10.0, of which ~299 are substantive).
  use: git
  filters:
    exclude:
      - '^pact: ledger sync'
      - '^pact [a-z0-9-]+: (checkpoint|accept|assign|ledger)'
      - '^Merge (f|[a-z-]+) \(feat/'
      - '^spec\('
      - '^docs\(backlog'
  groups:
    - title: Features
      regexp: '^feat'
      order: 0
    - title: Fixes
      regexp: '^fix'
      order: 1
    - title: Security
      regexp: '^security'
      order: 2
    - title: Other
      order: 99
